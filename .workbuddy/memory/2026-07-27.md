# 2026-07-27

## 将 Cursor `interview` 技能改写为 WorkBuddy 项目级技能
- 源文件：`E:\Java-notes\.cursor\skills\interview\SKILL.md`（Cursor 格式，含 `disable-model-invocation: true`）
- 目标：`E:\Java-notes\.workbuddy\skills\interview\SKILL.md`（WorkBuddy 格式）
- 适配要点：
  - frontmatter 改为 WorkBuddy 格式：`name` + 富触发词 `description`（第三人人称、含 /interview、模拟面试等触发词）+ `agent_created: true`
  - 删除 Cursor 特有字段 `disable-model-invocation`
  - 关键坑：`description:` 不能用 `>-` 折叠标量（其中的 `>` 会被校验器判为尖括号），改用 `|-` 折叠标量通过校验
  - 全文去掉 `<...>` 占位符（校验器对 description 禁尖括号，且 `>-` 会误把正文尖括号算进 description）
  - 正文改为祈使句/不定式风格；保留全部流程：逐题提问、40 分制评分、Progress Sync（INDEX/weak-points/sessions/question-bank）、git 跨设备续接、开场/追问/阶段/终局格式、评分 rubric
  - 新增「不懂先辅导不计分」规则（用户偏好）
- 用 skill-creator 的 `package_skill.py` 校验通过，并产出 `interview.zip`（已放在项目 skills 目录内，可分享）
- 该技能已可直接在 WorkBuddy 中用 `/interview` 或「模拟面试」等触发

## Java 基础篇模拟面试进度（本会话）
- 已读 INDEX / weak-points / chapter1-question-bank / sessions(至 Q74)
- 当前应从 Q75（P02 三种工厂模式）继续；新题剩 P02/P03/M02，之后做薄弱复考
- 用户中途插入「先把 interview 改写成 WorkBuddy 技能」，已完成；下一步回到 Q75 继续面试
