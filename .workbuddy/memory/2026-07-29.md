# 2026-07-29

## Q76 策略模式（P03）正式考 + 进度文件更新
- 用户经辅导后正式回答 Q76，得分 **27/40（一般）**。
- 答对：行为型设计模式、开闭原则 OCP、加分项「何时不用」（少数固定 if-else 用策略模式更麻烦）。
- 纠正 4 点：① 定义不完整（漏「运行时互相替换」）；② 三角色（Strategy/ConcreteStrategy/Context）未说全；③ 与 if-else 区别讲浅（漏运行时切换、可单测）；④ ⚠️ **工厂 vs 策略分工说反了**（工厂=创建哪个对象；策略=用哪个算法/行为）。
- **补登 weak-points #16**：上一轮（07-28 末）我口头声称已把策略模式写入 #16，实际漏了 Edit；本轮真正补入，标注「重要考点 + 工厂/策略混淆 gap」，纳入复考。
- 已更新四文件：INDEX（已答 76、累计 2034/3000、下一题 Q77=M02 native/JNI、续接指令同步）、session（追加 Q76 段）、chapter1-question-bank（P03 ✅、真正未考新题 2→1、已答 75→76、下一题 M02）、weak-points（#16）。
- git：已 `commit`（未 push）。本 shell 无头凭据 GUI 弹不出，push 仍须用户在助理面板执行。
- 剩余扫尾：新题 M02；之后薄弱复考约 8～11（含 #16 策略模式、#15 抽象工厂 OCP 等）。

## Q76 策略模式复考（用户主动要求）
- 用户自述「工厂创建、策略选算法」已说对（Q76 最关键错误已纠正），自认不熟，主动要求复考。
- 复考场景「支付方式选择」：用户答三角色（策略=支付接口 / 具体策略=独立类 / 上下文持有引用运行时调）+ 协作流程（set 选 + 调方法）全对，得分 **32/40（一般→良好）**。
- 残留 gap：① 只口述未现场写代码；② 小口误「客户端在上下文持有引用」应为「Context 持有、客户端注入」。
- 结论：策略模式概念已掌握，weak-points #16 升级为「已复考·基本掌握」，复考名单降优先级。
- 已更新：session 追加复考段、weak-points #16（re-exam 表现 + 动作）、INDEX（最后更新行 + 设计模式覆盖行注「复考掌握」）。
- git：本地提交 `67a0281`（连同 `bb8dc36` 共 2 个 commit 待 push；本 shell 仍无法 push，需助理面板执行）。

## 用户真实项目案例：动态 S3 客户端选择器（策略模式实战）
- 形态：DB 配置多 bucket/AKSK，启动时注册进内存 map（ID→客户端），运行时按 clientId 通过"上下文"切换；全局单门面，内部路由到不同实际 S3 客户端。
- 本质：**策略模式 + Registry** 的生产落地（Strategy 接口=S3 操作；ConcreteStrategy=各配置实例；Context=门面+当前 clientId；Client=启动注册逻辑）。
- 架构提醒（已告知用户）：上下文须线程/请求级隔离（ThreadLocal/request-scoped），避免并发串号。
- 价值：可作为设计模式面试题的**实战素材**；确认用户对策略模式理论↔实战已完全打通。后续面试辅导可引用此例。
- 实际机制（用户补充）：`runWith(clientId, action)` 内用 ThreadLocal 上下文持有者 set 指定客户端、执行 action、finally 中 restore/remove；作用域 = 单次 runWith 调用（≤ per-request），共享线程池下无串号风险。**这是正确实现，不是'进程级全局可变'反例**；未来辅导只需提醒：确保 finally 清理（建议保存 prev 并 restore 以支持嵌套 runWith）。

## push 打通（用户提供 GitHub PAT）
- 用户提供了 GitHub PAT（ghp_ 开头）；按既定方案配置**项目级 local** credential helper（`git config --local credential.helper '!f() {...}'`），只影响本仓库、不污染全局。
- 成功 push：`448a467..67a0281 main -> main`（EXIT=0），3 笔挂起 commit（448a467 收尾核对 / bb8dc36 Q76 首答 / 67a0281 Q76 复考）全部上远程，待推送为空。本 shell 无头 push 问题已解决。
- ⚠️ 安全：PAT 明文写入 `E:/Java-notes/.git/config`（local 段）。已提醒用户：① 去 GitHub Settings→Developer settings→PAT 撤销该 token；② 不再需要无头 push 时 `git config --local --unset credential.helper` 清除。
- 备注：remote owner 是 **ZAcoooo**（非 SouAs）；PAT 仅本项目可用。

## Q77 native/JNI 收尾（新题全部答完）
- 用户答 **30/40（良好）**：主干全对（native 无方法体 / 编译生成 .h / 写 .c / JNI 双向桥含「回调 JVM」亮点 / 拖垮 JVM + 底层用 C）；缺口：① 调用流程缺 `System.loadLibrary` 这一步 + 漏编译成动态库；② 代价漏边界开销(JIT 不可跨边界内联)+内存手动管理；③ 好处漏「复用已有 C/C++ 库」。
- 已登记 weak-points **#17**（JNI 代价/好处细节 + loadLibrary 步骤，🟡 中优先级；复考聚焦细节不必重考主干）。
- 四文件更新：INDEX（已答 77、累计 2064/3000=69%、状态转薄弱复考、下一题 Q78=复考#0 编译/解释/JIT）、session（Q77 段）、question-bank（M02 ✅/真正未考新题 1→0/已答 76→77）、weak-points（#17）。
- git：commit `2901a2a`，已 push（`67a0281..2901a2a` main，EXIT=0，PAT local helper 生效）。
- 阶段结论：**M02 收 → 新题全部答完**；进入薄弱复考（约 8~11 题），下一题优先复考 #0 编译/解释/JIT（🔴 高）。
