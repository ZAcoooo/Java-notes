# 2026-07-30 工作记忆

## 模拟面试 Java 基础篇（收尾阶段 · 薄弱复考）

- **Q79 复考 #1 泛型与泛型擦除：34/40（良好·通过）**。用户主干全对，且主动答出「为什么擦除=兼容 Java 1.4」，往年 Q9(11)/Q42(33)/Q43(32) 没答出此点。残留 gap：① 因果小瑕（"类型安全因擦除"应为"先编译期检查再擦除兼容"）；② 漏重载冲突（f(List<String>) 与 f(List<Integer>) 擦除后签名同）；③ instanceof List<String> 不行（运行时无泛型信息）——已现场讲清。weak-points #1 升级为「已掌握出池」。
- **累计**：79 题，2152/3000（72%）。复考池 10→9（#0/#1 出池）。
- **下一题**：Q80 = 复考 #2 final 与不可变类（🔴 高优先级，weak-points #2）。
- 用户学习特点：对「类型参数化」「盒子/容器」比喻接受度高；易在「因果表述」上口误；抽象概念需先给生活比喻再上代码。

## git / 凭据（沿用 07-29 决策）
- PAT 已配为项目级 local credential.helper（仅 E:/Java-notes，未污染全局），本 shell 可直接 push。
- 之前挂起的 commit（448a467/bb8dc36/67a0281）已于 07-29 推送成功；Q77(2901a2a)/Q78(c5c5bf4)/Q78二刷(21bae6b) 均已 push。
- ⚠️ 收尾还剩约 9 道复考；PAT 明文仍在 .git/config，**收尾全部完成后**执行 `git config --local --unset credential.helper` 清掉，并提醒用户去 GitHub 撤销该 token。
